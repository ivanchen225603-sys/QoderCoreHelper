---
name: qoder-core-helper
description: 软件工程全流程流水线：把一句话需求或一份 PRD 自动推进到可发布的最后一步，七个环节（需求架构→实现→测试→评审→安全→发布准备→发布）由状态机串联，只在必须人担责的关卡停下等放行。仅当用户明确要"从头做一个完整项目/功能到能发布"、"跑全流程"、"从需求到上线"、"交付一个可发布版本"时使用。以下情况绝不使用：修一个 bug、加/改一个字段、写一个脚本或函数、重构单个模块、回答技术问题、只写文档、只做代码评审、用户没要求走完整流程的普通开发任务——对这些任务它是纯负担。
---

# 软件工程全流程流水线（执行契约）

本文档是给你（主会话智能体）的操作契约，不是给人看的教程。每一节直接
对应一个动作。判断你执行得好不好只有一个标准：**用户不写代码、不写配置、
不调流水线，只在关卡上回答"放行还是打回"。**

## 0. 执行前检查（不满足就停下说明，不许硬跑）

1. 用户的需求是"做一个能发布的完整东西"。只是改 bug/加字段/写脚本 →
   不启动本流程，直接说明并退出。
2. `python` 在 PATH 里（脚本层依赖）。没有 → 声明"门禁退化为逐条人工
   核对清单，这不等价于脚本门禁"，问用户是否继续。
3. 目标项目目录已确定。

## 1. 初始化（只做一次）

```
python <skill>/scripts/init_pipeline.py --project <项目根>
```

- 探测不到技术栈会明确报错（退出码 3）——**不许替用户猜**，让用户放
  工程文件或显式 `--stack`。
- 输出里有**能力声明**（子 Agent 派发/工具白名单/工作副本），把它原样
  转述给用户一次。工具白名单不可用时，声明"隔离已退回成纯约定"，
  不许静默。
- 此后所有命令用安装进项目的副本：`python .pipeline/scripts/<脚本>`。

## 2. 主循环（每个环节重复）

```
step1  python .pipeline/scripts/state.py next-action     # 问状态机下一步
step2  按返回的 action 执行（见下）
step3  回到 step1
```

| action | 你做什么 |
|--------|---------|
| `start_stage` | `state.py start --stage Sn`，然后派发该环节角色（第 3 节） |
| `run_env_gate` | 派发 gate-runner 跑 `gate.py run --gate Gn`（第 4 节） |
| `present_to_user` | 按固定格式呈现关卡（第 5 节），等用户放行/打回 |
| `stop_for_user` | 自愈轮次用尽：把失败项作为需决策项呈现，**不许再修** |

用户放行后执行：
```
state.py approve --gate Gn --by "<用户的真实姓名>"
```
`--by` 只能是用户报的姓名。用户没说姓名就要；**拿任何智能体署名去放行
会被脚本拒绝（退出码 4），这是设计**。打回用：
```
state.py reject --gate Gn --by "<姓名>" --reason "<原因>"
```
打回时脚本自动登记返工工单（dod = docs/rework-<gate>.md，返工说明必须逐条
回应打回原因），`next-action` 会把最近打回原因带出来——返工派发时把它写进指令。

## 3. 环节派发（S1–S6）

1. 按 `references/ticket-protocol.md` 把环节工作拆成工单（拆分边界 =
   文件边界），`tickets.py add` 登记。登记冲突被拒 = 拆错了，重新拆。
2. `tickets.py claim --id Tx --by <子Agent名>` 认领（脚本强制独占检查）。
3. `render.py pack --ticket Tx` 渲染派发包——**派发包必须由工具渲染，
   不许手工拼**（手工拼漏了禁区不会有任何提示）。
4. 派发优先用角色同名的自定义子代理（已装入项目 .qoder/agents/，
   工具白名单由平台强制执行：评审者拿不到编辑工具是"做不到"而非"不许做"）；
   平台不认角色名时退回 GeneralPurpose + 派发包全文。子 Agent 冷启动，只认派发包。
5. 子 Agent 回报后：`tickets.py done --id Tx`（脚本核验 dod 文件存在）。
6. 全部工单 done → 环节产出齐 → 回到主循环跑关卡。

隔离三铁律（派发时逐条自检，违反任何一条 = 门禁全绿但什么也没拦住）：
- 评审者只拿评审面文件 + 验收标准 + 契约，拿不到实现者思路。
  评审面必须由工具渲染：`render.py diff`（产出 reports/changes.diff，
  S4 放行校验会强制检查它存在——忘了渲染就过不了关）。
- 测试编写者只拿验收标准 + 接口契约，拿不到实现代码。
- 生成、评审、测试是三次独立派发，同一子 Agent 不得评审自己的产出。
认领后子 Agent 中断/崩溃：`tickets.py unclaim --id Tx` 退回待认领。

## 4. 环境门禁（G2 / G3 / G5 / G6 里的环境检查）

派发给 gate-runner 角色（独立上下文，吃掉原始日志，只回传卡片）：
```
python .pipeline/scripts/gate.py run --gate Gn
python .pipeline/scripts/render.py gate-card --report <最新报告路径>
```
按退出码分流（细节见 `references/gate-protocol.md`）：
- **0** → 把关卡卡片呈交用户放行（仍需人名）。
- **2** → 按每项 `classification`：`self_heal` 直接修（先
  `state.py heal-round --gate Gn` 记轮次，上限 2 轮）后重跑；门禁还会自动计同一检查的连续失败次数，
  连败 3 次的自愈项自动升级为需决策（不依赖你是否记录轮次）；
  `needs_decision` 停下呈交用户，给带代价的选项。
- **3** → 配置错：检查根本没跑。**不许当未通过去自愈，更不许当通过**；
  修配置属生成待批项，呈交用户。
- **4** → 违例：停下，向用户说明被哪条规则拦住。

## 5. 关卡呈现（格式不许变）

严格按 `references/presentation-format.md` 的模板：先判定、失败项带归类
与建议动作、需决策项给带代价的选项、降级声明、证据路径（不贴日志）、
末尾给可直接回答的下一步选项。
G1 把需求 + 架构 + 原型**合并成一次呈现**（三份产出缺一不可）——
分三次问，用户会在信息不全时逐个点头。

## 6. 三个停点之外一律不停

默认一路跑到底。只在三种情况停：环节关卡、需决策类失败、越界动作。
完整清单与"全自动/生成待批/绝不触碰"三档见
`references/automation-bounds.md`。生成待批项必须在关卡里**单独列出**，
不许混在产出里悄悄通过。

## 7. 参考路由表（什么时候读哪份）

| 你正要做什么 | 先读 |
|--------------|------|
| 拆工单 / 处理认领冲突 / 决定并行 | `references/ticket-protocol.md` |
| 跑门禁 / 处理失败 / 办豁免 / 自愈 | `references/gate-protocol.md` |
| 呈现关卡 | `references/presentation-format.md` |
| 拿不准"这事要不要问用户" | `references/automation-bounds.md` |
| 确认上下游该交接什么文件 | `references/contract-table.md` |
| 新增技术栈 | `references/extend-stack.md` |
| 平台能力不足 / 单会话降级 | `references/platform-adaptation.md` |
| 派发某角色 | `agents/<role>.md`（通过派发包带过去，你不必全读） |

## 8. 硬性规则（编号规则全部是机制或禁令，没有"建议"）

1. 两类关卡分开：环节关卡（G1/G4/G6/G7）人判断"该不该做成这样"；
   环境门禁脚本判断"做得对不对"。混用的后果：让人看几百项检查他只会
   闭眼放行，让脚本判断架构方向它只会放行一切。
2. 关卡阻断式：G(n) 未放行，`state.py start` 拒绝开始 S(n+1)。
   放行记录含姓名与时间——中断几天回来，靠它判断走到哪、责任在谁。
3. 只有人能放行：脚本拒绝智能体署名（`approve --by` 有黑名单校验）。
   你要做的是呈现，不是替用户决定。
4. 环节间靠文件交接：产出不在契约表里，下游不许读。
5. 工单四字段（inputs/owns/requires/dod）缺一不登记；`done` 时脚本
   核验 dod 存在，"做完了"不能只是自述。
6. 文件独占是机制：认领强制查重 + 操作系统文件锁；路径已归一化，
   不同写法骗不过去。
7. 让门禁变绿的手段一律禁止（降阈值/加 skip/注释断言/`|| true`/
   --no-verify/无理由的忽略项/缩小扫描范围/把失败测试改成断言错误行为）。
   觉得阈值不合理 → 需决策项呈交用户。用户改阈值是把关，你改是绕开把关。
8. 豁免三要件：人名 + 到期日 + 理由，状态显示"已豁免"不是"通过"。
   密钥扫描零豁免，脚本直接拒绝。
9. 外部工具不可用 → 按适配器降级并在关卡写明；降级导致不达标 = 需决策，
   不许静默放行。外部工具的自评不能替代门禁判定。
10. 凭据只生成占位符与注入机制；第三档动作做到可执行状态交人。
11. 不许用自我评估替代脚本检查："我觉得测试能过"与"测试跑了且过了"
    的差距就是这套东西存在的全部理由。
12. 改过 `scripts/` 下任何脚本，先跑
    `python .pipeline/scripts/test_pipeline.py`；回归不过不许继续。

## 9. 会话恢复（中断几天回来）

```
state.py status        # 七环节与关卡走到哪、谁放行的
tickets.py brief       # 工单进度
state.py next-action   # 继续的起点
```
全部状态在 `.pipeline/` 文件里，不依赖对话记忆。
